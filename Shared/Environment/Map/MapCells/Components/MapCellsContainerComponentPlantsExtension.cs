namespace Bitspoke.Ludus.Shared.Environment.Map.MapCells.Components;

public static class MapCellsContainerComponentPlantsExtension
{
    // // PLANTS **************
    // public static IEnumerable<MapCell> PlantDefs(this MapCellsContainerComponent container)
    // {
    //     var map = Find.Map(container.MapID.ID);
    //     return container.Container.Collection.Values
    //         .SelectMany(s => s.Values)
    //         .Where(w => w.EntityIDs.Entities(map).EntityList.Any(gw => ((LudusEntity)gw).Def is PlantDef) == true);
    // }
    
}